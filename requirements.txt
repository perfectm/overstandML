# Core data stack
pandas>=2.2
numpy>=1.26
pyarrow>=16.0

# Models (spec §2: LGBM | XGB toggle)
lightgbm>=4.3
xgboost>=2.0
scikit-learn>=1.4

# Hyperparameter optimization (spec §6: <=50 trials per study)
optuna>=3.6

# Market data for features (spec §4: VIX, VIX9D, VIX3M, VIX6M, SPX open/gap)
yfinance>=0.2.40

# App / visualization (reference system inferred to be Plotly Dash, spec §2)
dash>=2.17
plotly>=5.22

# Utilities
python-dateutil>=2.9
pyyaml>=6.0
tqdm>=4.66

# Dev / tests
pytest>=8.0
